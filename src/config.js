
const API_URL = 'https://jsonplaceholder.typicode.com/photos/';

// e.g. API endpoint
// https://jsonplaceholder.typicode.com/photos?_start=0&_limit=8

const IMAGE_BASE_URL ='https://via.placeholder.com/';

const IMAGE_SIZES = new Set([200, 300, 400, 600]);

export {
  API_URL,
  IMAGE_BASE_URL,
  IMAGE_SIZES
};
